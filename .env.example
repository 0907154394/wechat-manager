# Copy thành .env hoặc cấu hình trực tiếp trên Render > Environment

# MongoDB connection string (bắt buộc trên production)
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/wechat

# Mật khẩu đăng nhập trang admin (PHẢI đổi trước khi deploy)
ADMIN_PASSWORD=pp778899

# Khóa bí mật để ký token (đừng thay đổi sau khi đã deploy)
SESSION_SECRET=7e0117526165be8e6dbfd5389cc2a98a111f11949e3aa443e9732864dd6a4d7f

# Port (Render tự set, không cần chỉnh)
# PORT=3000
